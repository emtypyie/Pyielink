[package]
name = "pyielink-gui"
version = "0.1.0-alpha.4"
edition = "2024"
description = "Native file-explorer viewer for pyielink remote sessions (thin launcher)"

[dependencies]
pyielink = { path = "../pyielink-rs", features = ["gui"] }

[profile.release]
opt-level = 3
lto = true
strip = true